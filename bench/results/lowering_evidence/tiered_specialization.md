# Tiered specialization (P1) -- IR-level evidence

Signal: `./examples/filtered_momentum.sig`

This artifact is auto-generated by `stateful_lowering_ir_diff`. It
compares the post-O2 LLVM IR of three configurations:

1. `lowering=none`  -- baseline P-1 (opaque `jit_rt_*` calls).
2. `lowering=all`   -- P0 IR lowering (calls expanded into typed IR
                        with full `is_init`/`is_full` guards).
3. `tier=specialized` -- P1 warm-loop specialization (lowering=all +
                          warm-safe stateful nodes stripped of their
                          warmup branches).

## Lowered runtime calls (post-O2)

| helper | none | all | specialized |
|--------|------|-----|-------------|
| `jit_rt_ema_alpha` | 6 | 0 | 0 |
| `jit_rt_sma*`      | 0 | 0 | 0 |
| `jit_rt_lag`       | 0 | 0 | 0 |
| `jit_rt_rolling_std` (control) | 2 | 0 | 0 |

Once `lowering=all` is enabled, opaque calls for the lowered ops
should drop to 0; the `rolling_std` row is a control (not lowered).
The `specialized` column is expected to match `all` for call counts
(specialization changes guards, not the lowered op itself).

## Warmup-guard primitives (post-O2)

| primitive | none | all | specialized |
|-----------|------|-----|-------------|
| `select`        | 0 | 14 | 8 |
| `icmp eq i64`   | 0 | 10 | 4 |

`select` counts the materialized warmup-branch results, and
`icmp eq i64` counts the `tick_index == 0` / `count == period`
guard comparisons. Both should drop sharply between `all` and
`specialized` -- that drop is the win P1 buys for the warm loop.
(Some `select`/`icmp eq` instances remain in the specialized IR
for stateful ops that are nested under conditional branches and
therefore are NOT warm-safe; those are left untouched on purpose.)
