# CLAIMS_MATRIX.md

| Claim | Evidence | Risk | Status |
|---|---|---|---|
| Custom signal DSL parser exists | `src/lexer.*`, `src/parser.*`, 6 example `.sig` files, parser_smoke_test | Low | **Verified** |
| DSL supports explicit scalar parameters (`param name = <number>`) in addition to signals | `ParseProgram` / `ProgramDef` in `src/signal_program.*`, parser surface in `src/parser.*`, `parser_smoke_test`, `dsl_formatter_roundtrip_test` | Medium | **Verified** |
| Interpreter execution path works | `src/interpreter.cpp`, interpreter_test, 8/8 tests pass | Low | **Verified** |
| LLVM ORC JIT path exists | `src/jit_compiler.cpp`: LLJIT, IRBuilder, O2 pipeline, symbol registration | Low | **Verified** |
| Emits optimized x86-64 machine code | PassBuilder O2, DumpLastIR available, fuzz_parity 200 signals pass | Low | **Verified** |
| Built-in function set (18 operators) | `docs/operator_taxonomy.md`: 4 stateless + 10 rolling-window (period-parameterized) + 4 stateful non-windowed (`ema_alpha`, `kalman1d`, `cross_*`); inventory backed by interpreter/JIT/runtime sources | Low | **Verified** |
| Math built-in JIT/interpreter parity for `abs`, `log`, and `sqrt` | `test/jit_test.cpp` includes direct single-signal parity checks for each math builtin plus a nested math expression. | Low | **Verified** |
| Parameterized signal gradients agree with central finite differences across stateless and stateful operators | `test/gradient_parity_test.cpp` checks fan-out, conditional stop-gradient, `abs`/`log`/`sqrt`, `lag`, `sma`, `ema_alpha`, `rolling_std`, `zscore`, `rolling_corr`, `rolling_beta`, `kalman1d`, `rolling_min/max`, `cross_*`, and whole fused programs against central FD (`h = 1e-6 * max(1, |theta|)`; rel `1e-4` or abs `1e-8` near zero). | Medium | **Verified** |
| Compiled whole-program gradients agree with interpreted gradients at tight tolerance | `test/gradient_parity_test.cpp` runs a three-way gate: interpreted gradient vs FD, compiled gradient vs interpreted gradient, and compiled value vs interpreted value. The compiled-vs-interpreted gradient gate is rel `1e-12` or abs `1e-12` near zero and covers operator cases plus whole programs including `ema_alpha` and `kalman1d`. | Medium | **Verified** |
| Fixture-backed calibration uses the compiled whole-program gradient path and measurably improves its objective | `cli/jitse_calibrate.cpp` calls `JitCompiler::CompileProgramGradient` and optimizes `examples/calibration_fit_target.sig` against `test/fixtures/calibration_ticks.csv`; `calibration_smoke_test` gates a fixed minimum improvement (`0.02`), and the checked-in artifact `bench/results/autodiff/calibration_fixture_fit.{md,meta.json}` records the measured run (`0.06167875 -> 0.0012323651`). | Medium | **Verified** |
| JIT/interpreter parity | fuzz_parity_test (200 signals x 50 ticks), jit_test (1000-tick program), 8/8 pass | Low | **Verified** |
| JIT throughput uplift over interpreter (pinned host) | `bench/run_pinned_speedup.sh` on canonical host `wsl2-ultra9-275hx-2026-05` (`bench/PINNED_HOST.md`): 30×1M-event repeats, core 2, bootstrap 95% CI on median speedup; **production default `StatefulLoweringFlags::kAll`**. Momentum `7.32x` [7.29x, 7.48x]; all-signals fused `15.56x` [15.52x, 15.58x]. Artifacts: `bench/results/pinned_host_speedup.{json,md}`. Historical lowering-off fused `2.96x`: `bench/results/lowering_gap_baseline/baseline_table.md`. | Medium | **Verified** |
| JIT throughput uplift over interpreter (historical, different host) | Prior WSL refresh (`bench/results.csv`, commit `cce2c3df0f9f9938308276919d1a7f1de4656cfb`): momentum `4.34x`, fused `5.43x`. Older environment: momentum `10.4x`, fused `13.2x`. Not the canonical pinned-host claim. The CSV predates the June autodiff source edits, but those edits do not change the forward-only benchmarked paths and the artifact is commit-pinned. | Medium | **Historical** |
| Single native eval per tick (CompileProgram) | CompileProgram emits all signals into one LLVM function; confirmed by jit_test 1000-tick parity | Low | **Verified** |
| Market-data read deduplication in fused JIT (per-tick field memoization) | Fused `filtered_momentum.sig` IR evidence: bid/ask loads **22 → 2** pre-O2 after emitter memoization (`bench/results/cse_evidence/cse_diff_verified.md`, `cse_load_dedup_test`). Prior pre-memo artifact (`cse_diff_report.md`) showed **22 → 22** (LLVM CSE alone insufficient across `jit_rt_*` calls). | Medium | **Verified** |
| JIT p99 latency between 13-78 ns | Fresh LLVM-enabled benchmark refresh (`bench/results.csv`, commit `cce2c3df0f9f9938308276919d1a7f1de4656cfb`) shows JIT p99 range `13-78 ns` across measured signals; max is `78 ns` (`<all_signals>` fused path). The CSV predates the June autodiff source edits, but those edits do not change the forward-only benchmarked paths and the artifact is commit-pinned. | Low | **Verified** |
| Explicit AVX2 vectorization in rolling-window JIT codegen with runtime feature detection and scalar fallback | `src/jit_compiler.cpp` adds AVX2-gated SIMD lowering for `sma`; `src/runtime.cpp` adds SIMD prep hook; detection via `JitCompiler::HasAVX2`; scalar fallback via `JITSE_FORCE_DISABLE_AVX2`; validated by `fuzz_parity_simd_test` and `bench/results_simd.csv`. On current host, `jit_has_avx2=true` and fallback mode remains functional. | Medium | **Verified** |
| Multi-symbol SoA evaluation with parity-preserving single-compile multi-instantiation | `MultiSymbolSignalContext` arena + symbol-indexed JIT entrypoints in `src/runtime.*` and `src/jit_compiler.*`; parity covered by `fuzz_parity_multisymbol_test` (K=64 deterministic streams) and retained `fuzz_parity_test` interpreter-vs-JIT oracle. | Medium | **Verified** |
| Scaling characteristics measured to 10K symbols | `bench/results_multisymbol.csv` (N={1,10,100,1000,10000}) captures total throughput, per-symbol throughput, and pass latency p50/p99 for one compiled program reused across symbols. Current run shows sub-linear scaling and higher p99 at large N, reported as measured behavior rather than universal claim. | Medium | **Verified** |
| Multi-threaded multi-symbol execution on hybrid pinned host (P+E cores mixed) | `bench/run_pinned_multithread_scaling.sh` (default cores 2–17); `multithread_equivalence_test`; `bench/results/multithread_scaling.{json,md,run.log}`. 10,000 symbols, 60s/run: **5.69x** at 16 threads vs 1 (**35.6%** efficiency); `wsl2-ultra9-275hx-2026-05`. | Medium | **Verified** |
| Multi-threaded multi-symbol execution on pinned host — P-cores only | `bench/run_pinned_multithread_scaling.sh --cores 1,2,3,4,5,6,7 --thread-counts 1,2,4,6`; `multithread_equivalence_test`; `bench/results/multithread_scaling_pcores.{json,md}`. 60s/run: **5.45x** at 6 threads vs 1 (**90.8%** efficiency). | Medium | **Verified** |
| Multi-threaded multi-symbol execution on pinned host — E-cores only | `bench/run_pinned_multithread_scaling.sh --cores 8,9,…,23 --thread-counts 1,2,4,8,16`; `multithread_equivalence_test`; `bench/results/multithread_scaling_ecores.{json,md}`. 60s/run: **6.51x** at 16 threads vs 1 (**40.7%** efficiency). | Medium | **Verified** |
| Backtest framework with deterministic IC computation on recorded market data | `examples/backtest_runner.cpp` consumes mdh canonical journal (`itch_1m_ab_source.journal`) and writes `bench/results/backtest/<run_id>/{signals.csv,ic_report.json}` with IC at horizons 1/5/30 ticks; `test/backtest_determinism_test.cpp` reruns identical config twice and asserts matching IC sections. This does not imply the recorded-data IC calibration objective is currently usable at HEAD. | Medium | **Verified** |
| JIT signal output compared against pandas reference oracle on recorded ITCH data and deterministic numeric fixture | `bench/run_diff_oracle.sh`; `test/reference_oracle/{generate_fixture_events.py,compute_reference.py,diff_signals.py,requirements.txt}`; `examples/backtest_runner.cpp`; `bench/results/diff_test/{divergence_report.json,divergence_report.md}`. CTest fixture gate: deterministic AAPL rows, `--min-within-rate 0.999`, 100.00% within tolerance for all five signals, including 211 numeric `vol` and `filtered` rows. Recorded 1M ITCH run (`recorded_1m_after_fixture_gate`, `--min-within-rate 0.99`): `short_ma`/`long_ma`/`raw` 100.00%, `vol` 99.74%, `filtered` 99.72% within tolerance (`rtol=1e-6`, `atol=1e-9`). Oracle exposed and regression-tests the cross-signal node-ID state collision fixed in whole-program compilation. | Medium | **Verified** |
| Preallocated per-node state, no hash-map on hot path | SignalContext uses std::vector indexed by integer node_id; unordered_map only at setup | Low | **Verified** |
| Stable node IDs with prebound symbol slots and node-indexed runtime state | `AllocateNodeIds` + `BindSymbolIds` in `src/signal_program.cpp`; `SignalContext` vector pools indexed by node ID in `src/runtime.h`; covered by `node_state_layout_test` | Low | **Verified** |
| Zero heap allocations during warmed evaluation loop | `hot_path_allocation_test` warms state, enables global allocation counter, and asserts zero allocations in the interpreter loop, forward JIT loop, and compiled forward+gradient JIT loop when available | Medium | **Verified** (JIT branches are conditional on LLVM availability) |
| Benchmark numbers are reproducible | bench/run_benchmarks.ps1 + bench/run_benchmarks.sh; fixed seed (42), 10K warmup, batch-64 timing | Low | **Verified** |
| `signal_core` builds with the current working tree | `cmake --build build --target signal_core --parallel 8` | Low | **Verified** |
| Cross-symbol vectorized JIT beats scalar JIT on FP-heavy stateless program | `bench/cross_symbol_benchmark.cpp`; `examples/stateless_compute_heavy.sig`; pinned P-core, K=4, best-of-15: **~2.62×** (`bench/results/avx2_speedup/stateless_compute_heavy.md`); bit-exact `vectorized_lanes_parity_test` case `compute_heavy_sqrt_chain` | Medium | **Verified** |
| Cross-symbol vectorized JIT modest win on memory-bound stateless program | Same harness; `stateless_heavy.sig`: **~1.36×** at K=4 (`bench/results/avx2_speedup/stateless_heavy.md`) | Medium | **Verified** |
| `rolling_std` uses O(1) Welford with periodic buffer refresh | `RingStatsStddevSample` in `src/runtime.cpp`; two-pass reference `RingStatsStddevSampleTwoPassReference`; `welford_stddev_parity_test` (benign ≤1e-12 rel, cancellation ≤1e-6 rel) | Low | **Verified** |
| P11 whole-program inlining stateful dedup (cross-signal / duplicate subtree) | `AllocateProgramNodeIds` structural dedup; `stateful_emit_cache` (JIT); `stateful_eval_cache_` (interp); `test/stateful_subtree_dedup_test.cpp` | Medium | **Verified** |
| Persistent JIT module cache (post-O2 bitcode) | `JitCompiler::EnableModuleCache`; deterministic cache key (`AstCanonicalString` + flags); `jit_module_cache_test` | Medium | **Verified** |
| SPSC ring FIFO + alloc-free hot path | `src/spsc_ring.h`; `spsc_ring_test` (1M messages, 0 allocs on 100k push/pop pairs) | Low | **Verified** |
| Live ingest pipeline latency (enqueue→signal) | `spsc_jit_pipeline_bench`; CO-aware pacing; p50 **~228 ns** spread / **~280 ns** filtered_momentum at 2 MHz on WSL2 pinned cores (`bench/results/spsc_pipeline/`) | Medium | **Verified** (p50; tails host-noise on WSL) |
| Tiered JIT baseline + warm specialization | `TieredProgramJit`; atomic fn swap; `tiered_specialization_parity_test`; `docs/tiered_specialization.md` | Medium | **Verified** |
| DSL formatter round-trip + lint frontend | `dsl_formatter_roundtrip_test`; `cli/jitse_fmt.cpp`, `cli/jitse_lint.cpp` | Low | **Verified** |
| `runtime_call_profile_test` gates P0 lowering drop | `test/runtime_call_profile_test.cpp`; workload 35M events; per-op absolute + relative gates | Low | **Verified** |
| JIT vs hand-written C++ gap (pinned host, per-signal hw baselines) | `bench/hw_reference.{h,cpp}` + `bench/run_lowering_gap_phase3.sh`; `bench/results/lowering_gap_phase3/phase3_table.md`. Fused `filtered_momentum` JIT÷hw **1.42×**; spread **1.05×**; momentum **0.95×**. Lowered-state bases via GEP (`bench/results/lowering_gap_phase3/ir/README.md`). Fresh profile: `jit_rt_*` **80.2% → 2.8%** on fused program (`bench/results/perf/filtered_momentum/runtime_call_profile.md`, 40M events). | Medium | **Verified** |
| Stateful lowering composable with cross-symbol vectorization (P4 parity) | `LaneEmitScope` per-lane lowered fan-out in `src/jit_compiler.cpp`; `vectorized_stateful_parity_test` (`kAll` cases), `vectorized_lanes_parity_test` (lowered compile cases), `fuzz_parity_simd_test`. Mutual-exclusion guard removed. | Low | **Verified** |
| Lowered+vectorized beats scalar-lowered on FP-heavy stateful program | `bench/results/stateful_vec_lowering_speedup.md` (`filtered_momentum`, K=4): vec+`kAll` **0.57×** vs scalar+`kAll`; **2.13×** vs vec+`kNone`. Per-lane lowered IR does not beat single-call scalar lowered; K-wide SIMD state deferred. | Medium | **Not achieved** (documented) |
